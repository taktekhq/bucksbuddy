node_modules/
.expo/
dist/
web-build/
expo-env.d.ts
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# The Google Play service account key. It should be uploaded to EAS and then
# deleted locally (LAUNCH.md §3) — this is the seatbelt, not the plan.
google-service-account*.json
play-service-account*.json
*-service-account*.json
.env
.env.local
.DS_Store

# test coverage output
coverage/

# per-developer agent tooling, like the repo root
.claude/

# Generated by `expo prebuild` / `expo run:*`. This app stays on the managed
# workflow — EAS regenerates these from app.json, so committing them would
# silently take over from the config plugins.
ios/
android/
